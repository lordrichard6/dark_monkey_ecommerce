// FUTURE FEATURE: Art gallery page — not launched yet.
// To restore: remove the notFound() below and revert this file from git history.
// Original full implementation is preserved in git (search commits for "art gallery").
import { notFound } from 'next/navigation'

export default function ArtPage() {
  notFound()
}
